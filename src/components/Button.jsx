const Button = ({ styles }) => (
   <button type="button" className={`bg-blue-gradient py-4 px-6 font-poppins font-medium text-[18px] text-primary outline-none ${styles} rounded-[10px]`}>
      Get Started
   </button>
);

export default Button